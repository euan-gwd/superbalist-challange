"use strict";

jQuery.noConflict();
(function ($) {
  "use strict";

  $(function () {
    var options = {
      selector: "img"
    };
    $(document).myKeyboardNavigatorPlugin(options);
  });
})(jQuery);